import 'package:flutter/material.dart';

class AppConstants {
  static BoxDecoration kBoxDecoration = BoxDecoration(
    color: const Color.fromARGB(255, 37, 72, 126),
    borderRadius: BorderRadius.circular(10),
  );

  static const TextStyle kNumStyle = TextStyle(
    color: Colors.white,
    fontSize: 30,
    fontWeight: FontWeight.bold,
  );
  static const TextStyle kTextStyle = TextStyle(
    color: Colors.white,
    fontSize: 15,
  );
}
